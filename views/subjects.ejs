<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Subject Page</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
    <script src="main.js"></script>
</head>
<body>
    <h1 style="text-align: center">Subject Data</h1>
    <a href="/subjects/addSubject">add subject</a>
    <table style="width:100%">
        <tr>
            <th>id</th>
            <th>Subject Name</th>
        </tr>
        <% if(subjectData.length != undefined){%>
            <% for(let index =0; index<subjectData.length; index++){ %>
                <tr>
                    <th><%= subjectData[index].id%></th>
                    <th><%= subjectData[index].subject_name%></th>
                    <th><a href="/subjects/editSubject?id=<%=subjectData[index].id%>">edit</a> | <a href="/subjects/deleteSubject?id=<%=subjectData[index].id%>">delete</a></th>
                </tr>
            <%}%>
        <%}else{%>
            <h3 style="text-align: center">There is no subject registered as of this moment!</h3>
        <%}%>

    </table>
</body>
</html>